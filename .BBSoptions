# The reason this package is marked as unsupported on win32 is that
# it depends on Java/rJava (via DeepPINCS). However it seems that Oracle
# no longer provides the JDK for 32-bit Windows:
# https://www.oracle.com/java/technologies/javase-jdk14-downloads.html
UnsupportedPlatforms: win32
